fastapi>=0.115.0
uvicorn[standard]>=0.32.0
anthropic>=0.49.0
httpx>=0.28.0
python-dotenv>=1.0.0
websockets>=13.0
# Also requires: ollama pull nomic-embed-text (for semantic memory)
chromadb>=0.6.0
psutil>=5.9.0
# After pip install, run: playwright install chromium
playwright>=1.40.0
# Phase 5b — Fernet symmetric encryption for credential manager
cryptography>=41.0.0
# Phase 5e — APScheduler for recurring/scheduled tasks
apscheduler>=3.10.0
# Phase 9c — watchdog for file system event monitoring
watchdog>=4.0.0
